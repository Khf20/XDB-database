# Bundled Apps

Optional web tools can live here.

- `apps/phpmyadmin` - Extract phpMyAdmin here to enable `http://localhost/phpmyadmin/`.

The Apache template renders the phpMyAdmin alias only when this folder exists.
